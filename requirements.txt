# --- Core dependencies ---

Pillow>=10.0.0           # Image processing library (open, resize, format convert, resolution detection)
imagehash>=4.3.1         # Perceptual hashing algorithms (aHash, pHash, dHash, etc.) for finding duplicates
pyheif>=0.6.1            # HEIC/HEIF image decoder required by Pillow to open .heic images

# --- Testing tools (for development only) ---

pytest>=7.4.0            # Unit testing framework

# --- Core dependencies ---

Pillow>=10.0.0             # Image processing library (open, resize, convert, etc.)
pillow-heif>=0.12.0        # HEIC/HEIF plugin for Pillow
imagehash>=4.3.1           # Perceptual hashing for image similarity

# --- Testing tools (for development only) ---

pytest>=7.4.0            # Unit testing framework
